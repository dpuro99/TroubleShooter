name: Deploy to GitHub Pages
on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # Set VITE_BASE to '/<repo-name>/' so assets resolve for project pages
    env:
      VITE_BASE: '/${{ github.event.repository.name }}/'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Add SPA fallback
        run: cp dist/index.html dist/404.html
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          # Use GH_PAGES_PAT if provided, otherwise fall back to the automatic GITHUB_TOKEN.
          # If you need administration-level Pages APIs, create `GH_PAGES_PAT` as a repo secret.
          github_token: ${{ secrets.GH_PAGES_PAT || github.token }}
          publish_dir: ./dist
      - name: Ensure Pages is published from gh-pages
        uses: actions/github-script@v6
        with:
          # Use GH_PAGES_PAT if provided, otherwise fall back to the automatic GITHUB_TOKEN.
          github-token: ${{ secrets.GH_PAGES_PAT || github.token }}
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            await github.rest.repos.createPagesSite({
              owner,
              repo,
              source: { branch: 'gh-pages', path: '/' }
            });
